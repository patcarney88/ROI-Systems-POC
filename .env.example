# =============================================================================
# ROI SYSTEMS POC - ENVIRONMENT CONFIGURATION
# =============================================================================
# SECURITY WARNING: Never commit actual secrets to version control!
# This is a template file - copy to .env and replace all placeholder values.
# =============================================================================

# Environment
NODE_ENV=development
ENVIRONMENT=local

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
# SECURITY: Generate a strong password using: openssl rand -base64 32
# REQUIRED: Minimum 24 characters, use letters, numbers, and special characters
DB_USER=roi_user
DB_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_24_CHARS
DB_NAME=roi_poc
DATABASE_URL=postgresql://roi_user:CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_24_CHARS@localhost:5432/roi_poc

# =============================================================================
# REDIS CACHE CONFIGURATION
# =============================================================================
# SECURITY: Generate a strong password using: openssl rand -base64 32
# REQUIRED: Minimum 24 characters
REDIS_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_24_CHARS
REDIS_URL=redis://default:CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_24_CHARS@localhost:6379

# =============================================================================
# JWT AUTHENTICATION CONFIGURATION
# =============================================================================
# CRITICAL SECURITY: Generate cryptographically secure secrets
#
# To generate secure secrets, run this command:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# REQUIREMENTS:
#   - JWT_SECRET: Minimum 64 characters (128 hex chars recommended)
#   - JWT_REFRESH_SECRET: Must be different from JWT_SECRET
#   - Never use example values in production
#   - Rotate secrets every 90 days
#
JWT_SECRET=REPLACE_WITH_64_CHAR_HEX_FROM_CRYPTO_RANDOMBYTES
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_64_CHAR_HEX_FROM_CRYPTO_RANDOMBYTES
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# =============================================================================
# FILE STORAGE CONFIGURATION (S3/MinIO)
# =============================================================================
# For local development, use MinIO (S3-compatible)
# For production, use AWS S3
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET_NAME=roi-systems-documents
S3_FORCE_PATH_STYLE=true

# AWS (for production)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_ENDPOINT=http://localhost:4566

# S3 Buckets
S3_DOCUMENTS_BUCKET=roi-poc-documents
S3_ML_MODELS_BUCKET=roi-poc-ml-models

# Elasticsearch
ELASTICSEARCH_URL=http://localhost:9200

# =============================================================================
# EMAIL CONFIGURATION (SendGrid)
# =============================================================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=REPLACE_WITH_YOUR_SENDGRID_API_KEY
FROM_EMAIL=noreply@roi-systems.com
FROM_NAME=ROI Systems

# Email (SMTP fallback)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@roi-systems.com

# API URLs
API_URL=http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql
NEXT_PUBLIC_WS_URL=ws://localhost:4000/graphql

# ML Service
ML_API_URL=http://localhost:8000

# Monitoring (Optional)
SENTRY_DSN=
DATADOG_API_KEY=

# Feature Flags
ENABLE_ML_FEATURES=true
ENABLE_EMAIL_CAMPAIGNS=true
ENABLE_ANALYTICS=true

# OAuth Providers (Optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# Stripe (Optional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Twilio (Optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=